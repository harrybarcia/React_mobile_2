import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    titre: {
        fontWeight: "bold",
        padding: 25,
        fontSize: 32,
        color: "#f44",
      },

})