import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    titre: {
        fontWeight: "bold",
        padding: 10,
        fontSize: 32,
        color: "#f44",
      },

})